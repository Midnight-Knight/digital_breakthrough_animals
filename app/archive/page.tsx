import Provider from '@/provider';
import ArchivePage from "@/pagesClient/archive";

export default function Archive() {
    return (
        <Provider>
            <ArchivePage />
        </Provider>
    );
}